#ifndef _STRINGS_H_
#define _STRINGS_H_

/**
 * Return the length of a null terminated string.
 */
int strlen(char *a);

/**
 * Verify if two strings are equal.
 */
int streq(char *a, char *b);

#endif
